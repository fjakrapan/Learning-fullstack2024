let a = 8; // let จะโดนแทนค่าได้ 

if (a = 10) { //ค่าที่แสดงออกจะเป็น if เพราะใช้ = อันเดียว จะเป็นการเปลี่ยนค่า a เป็น 10 และค่า if จะเป้น ture โดยทันที
    // การเทียบค่าต้องใช้ == 2 ตัว
    console.log('if');
} else {
    console.log('else');
}


//แบบนี้ต่างชาตินิยมใช้ จะสั้น แต่จะใช้แบบไหนก็ได้แล้วแต่สะดวก
if (a = 10) console.log('if');
//แต่ถ้ามี console.log('') อีกอันนึงจะ error ทันที
else console.log('else');